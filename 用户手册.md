# 微信自动回复助手 · 用户手册

> 适用版本：基本功能开发完毕版（macOS 实测通过，Windows 同理可用）
> 面向对象：不需要任何编程基础，跟着做即可

---

## 目录

- 一、这是什么东西
- 二、开始前的准备
- 三、第一次使用（跟着做一遍）
- 四、每天怎么用
- 五、功能详解
- 六、出了问题怎么办
- 七、重要提醒
- 八、附录：配置速查表

---

## 一、这是什么东西

### 它能干什么

这是一个运行在电脑上的小程序，可以**代替你自动回复微信消息**。

装上并运行后，它会：

- 盯着你的微信电脑版，发现有人发消息就来告诉你
- 自己读懂对方说了什么
- 让 AI 想好该怎么回复
- 模仿人打字的方式，把回复一条条发出去

而且**不是机器人式的秒回**——它会把长回复拆成几句话，每句之间随隔 1~3 秒发送，看起来就像真人正在打字。

### 它怎么工作的（白话版）

你可以把它想象成**一个坐在你电脑前帮你回消息的人**：

| 它做的事 | 相当于人在做什么 |
|---|---|
| 每隔几秒给屏幕拍张照片 | 用眼睛看屏幕 |
| 认出照片上的字、判断是谁发的 | 读懂聊天内容 |
| 把内容发给 AI 思考怎么回 | 在脑子里想怎么回答 |
| 复制文字、粘贴、按回车 | 打字并发送 |

**关键点**：整个过程只是"看屏幕 + 动鼠标 + 敲键盘"，**完全没有修改微信本身**，也没有用微信的后台接口。就像你自己手动回复一样，所以**不存在封号风险**。

### 它能做到什么程度

**能做到的**

- 自动回复指定联系人的消息（谁要回、谁不回，你说了算）
- 给不同人用不同的"性格"回复（比如对女朋友温柔、对同事干练）
- 记住每个人的情况（家乡、生日、喜好等），聊天时能"回忆"起来
- 回复时可以自动发表情包
- 对方发表情包时，也能理解是什么意思并回复
- 同时盯着所有联系人，谁发消息就先处理谁

**做不到的（请提前知道）**

- **不能识别语音、视频通话**
- **看不了朋友圈**
- **无法主动找人聊天**——只有在别人发消息给你时才回复
- 如果你的微信窗口被完全遮挡、或者电脑锁屏了，它就"看不见"了
- 机器性能较差时，识别会稍慢（每几秒一轮）

---

## 二、开始前的准备

### 2.1 你需要准备这些东西

| 需要 | 说明 | 是否必须 |
|---|---|---|
| 一台 Mac 或 Windows 电脑 | 不能是手机、平板 | 必须 |
| 电脑版微信 | 并且要在电脑上登录好 | 必须 |
| Python 运行环境 | 版本 3.9 ~ 3.11，**推荐 3.10** | 必须 |
| 一个 DeepSeek 账号 | 用来生成回复，注册很简单 | 必须 |
| DeepSeek 里的余额 | 按用量扣费，聊得越多扣得越多 | 必须 |
| 一个能看图的 AI 账号 | 用来看懂别人发的表情包 | 可选 |

### 2.2 申请 DeepSeek 密钥（就是一串密码）

这是让 AI 帮你写回复的"钥匙"，跟着做：

1. 打开浏览器，访问 **https://platform.deepseek.com**
2. 用手机号注册并登录
3. 进入后找左侧菜单的 **「API Keys」**（接口密钥）
4. 点 **「创建 API Key」**
5. 它会给你一串以 `sk-` 开头的字符——**这就是密钥，马上复制保存好**
   - 【注意】这串字符只完整显示这一次，关掉就再也看不到了，务必先存到备忘录里
6. 在同一个网站的 **「充值」** 页面充一点钱（新用户通常有赠送额度，一般够用很久）

> 【提示】**费用参考**：日常聊天的话，几块钱能用很久。它会自动省着用——只把最近几句话发给 AI，不是每次都把全部聊天记录发过去。

### 2.3 安装 Python

**Mac 用户**：

Mac 通常自带 Python，但版本可能不合适。建议这样装：

1. 打开浏览器访问 **https://www.python.org/downloads/**
2. 下载 Python **3.10** 或 3.11 的安装包
3. 双击安装，一路点「继续」直到完成

**Windows 用户**：

1. 同样到 https://www.python.org/downloads/ 下载 3.10 或 3.11
2. 运行时**务必勾选最下面的「Add Python to PATH」**（添加到环境变量），否则后面命令会找不到
3. 点「Install Now」完成安装

**验证是否装好**：

打开「终端」（Mac）或「命令提示符」（Windows），输入：

```
python3 --version
```

看到显示 `Python 3.10.x` 或类似字样，就说明装好了。

### 2.4 下载本程序

如果你是从别人那里拿到的文件夹，跳过这步。

如果需要自己下载，在终端里执行：

```
git clone <项目地址>
cd WeChat-DeepSeek-Auto-Response
```

或者直接下载压缩包解压。

### 2.5 安装依赖（重要，做一次就行）

依赖就是程序运行需要的各种"零件"。

打开终端，**先进入程序所在的文件夹**。比如程序放在桌面的 `WeChat-DeepSeek-Auto-Response` 文件夹里：

```
cd ~/Desktop/WeChat-DeepSeek-Auto-Response
```

> 【提示】小技巧：在 Mac 上可以先输入 `cd ` （cd 后面有个空格），然后把文件夹直接拖到终端窗口里，路径就自动填好了。

然后执行：

```
pip3 install -r requirements.txt
```

**会下载不少东西，可能需要 5~15 分钟**，其中有个叫 OCR 的"认字引擎"比较大。看到滚动的文字停下来、回到命令提示符，就是装好了。

> 【注意】如果提示 `pip3: command not found`，试试把 `pip3` 换成 `pip`。

### 2.6 Mac 用户必做：授权（Windows 跳过）

Mac 系统管得严，程序要"看屏幕"必须你点头同意。

**第一步：授予「屏幕录制」权限**

1. 打开 **系统设置 → 隐私与安全性 → 屏幕录制**
2. 把你要运行这个程序的 App 勾选上
   - 如果用「终端」运行 → 勾选**终端**
   - 如果用「iTerm」→ 勾选 iTerm
   - 如果用 VS Code / PyCharm → 勾选对应软件
3. 如果列表里没有，点下面的 **+** 号手动添加

**第二步：授予「辅助功能」权限**

同样的位置，进 **「辅助功能」**，把同一个 App 勾选上（程序需要它来模拟打字）。

**第三步：完全退出再重新打开**

【注意】**授权后，必须把终端（或你用的软件）完全退出，再重新打开**，权限才会生效。这一步很多人漏掉，导致一直提示没权限。

> 【提示】macOS 14/15 之后，系统会时不时弹窗要求再次确认权限，看到弹窗点「允许」即可。

---

## 三、第一次使用（跟着做一遍）

### 3.1 填写配置文件

1. 在程序文件夹里，找到名为 **`.env.example`** 的文件
2. **复制一份**，把副本改名为 **`.env`**（注意前面有个点）
   - Mac 上文件名以点开头的文件默认是隐藏的，按 `Command + Shift + .` 可以显示出来
3. 用文本编辑器打开 `.env`，填入你的密钥：

```
OPENAI_API_KEY=sk-这里粘贴你刚才保存的密钥
OPENAI_BASE_URL=https://api.deepseek.com
OPENAI_MODEL=deepseek-chat
```

**只改第一行**，`sk-` 后面换成你自己的那串字符，其他的都不要动。保存文件。

> 🔒 这个文件保存了你的密钥，千万不要发给别人或上传到网上。

### 3.2 启动程序

在终端里（确保已进入程序文件夹），输入：

```
python3 wechatbot_gui.py
```

稍等几秒，会弹出一个窗口，标题是**「微信自动回复机器人」**。这就是操作界面。

> 【提示】以后每次用都是这样：进文件夹 → 输这条命令 → 出窗口。

### 3.3 框选两个区域（最关键的一步）

程序需要你告诉它"去屏幕的哪个地方看东西"。要点两个按钮：

#### ① 框选「联系人列表」

点界面上方的 **「① 框选联系人列表」** 按钮。

- 屏幕会定格成一张截图，鼠标变成十字
- **按住鼠标左键，拖出一个矩形**，把左边的好友列表框住
- 松开鼠标就完成了
- 框歪了没关系，按 `Esc` 取消重来

```
┌─────────────┬──────────────────────┐
│ 微信        │                      │
├─────────────┼──────────────────────┤
│ ┌─────────┐ │                      │
│ │ 张三    │ │   这里是聊天区域      │
│ │ 李四 ③  │ │                      │
│ │ 王五    │ │                      │
│ └─────────┘ │                      │
│  ↑框这里    │                      │
└─────────────┴──────────────────────┘
```

**框选要点**：把好友名字那一列框进去就行，上下多留一点没关系。

#### ② 框选「聊天窗口」

点 **「② 框选聊天窗口」** 按钮，同样拖出一个矩形。

```
┌─────────────┬────────────────────────────┐
│ 好友列表    │  张三                       │
│             ├────────────────────────────┤
│             │ ┌────────────────────────┐ │
│             │ │  [头像] 你好呀         │ │ ← 头像不要框进来
│             │ │                        │ │
│             │ │      我很好[头像]      │ │ ← 右边头像也不要
│             │ └────────────────────────┘ │
│             │ ┌────────────────────────┐ │
│             │ │ 请输入消息...          │ │ ← 输入框不要框进来
│             │ └────────────────────────┘ │
└─────────────┴────────────────────────────┘
```

【注意】**三个绝对不要框进去的东西**（非常重要）：

| 不要框 | 原因 |
|---|---|
| **左边的头像** | 会干扰程序判断消息位置 |
| **右边的头像** | 同上 |
| **最下面的输入框** | 会被误认成一条消息 |

**正确的框法**：只框住**中间那些聊天气泡**的区域。左右留出头像的空间，底部切到输入框上方。

> 【提示】框选结果会**自动记住**（存在 `config/regions.json`）。下次打开程序不用重选，除非你换了显示器或挪动了微信窗口位置。

### 3.4 启动自动回复

1. **先手动点一下微信的输入框**，让光标在里面闪
   - 这一步很重要！程序是靠"粘贴 + 回车"来发送的，光标没在输入框里就会发不出去
2. 回到程序界面，点 **「「启动自动回复」」**
3. 看下面的状态栏，显示 **「状态：运行中」** 就成功了

第一次启动会**加载认字引擎，可能要等十几秒到半分钟**，耐心一点。

### 3.5 观察它工作

界面中间的大片区域是**运行日志**，会实时滚动显示它在干什么：

```
==================================================
正在检查截图权限...
正在加载 OCR 模型（首次运行需要下载模型，较慢）...
启动完成，进入自动回复主循环。
[张三] 朋友发来新消息：在吗
[张三] 人设「暖心」，携带 4 条历史上下文
[张三] AI回答：在的在的，怎么啦
[张三] 已发送第 1/1 句：在的在的，怎么啦
```

看到这样的内容，说明它已经在正常工作了。

### 3.6 停止

点 **「「停止」」** 按钮。它会把手头这一轮处理完再停下，需要几秒钟。

---

## 四、每天怎么用

正常情况就这么三步：

```
1. 打开终端，进入程序文件夹
2. 输入 python3 wechatbot_gui.py
3. 点「「启动自动回复」」
```

**使用期间请注意**：

- 【注意】**运行期间不要动鼠标和键盘**！程序是靠模拟点击和打字工作的，你在中间操作会打断它
- 【注意】**不要把微信窗口最小化或遮挡**，程序需要"看得见"才知道有没有新消息
- 【注意】**电脑不要锁屏或休眠**（可以设置成"永不休眠"）
- 【提示】建议专门找个时间开着它，比如工作时把它挂在一边
- 【提示】想边用电脑边让它工作的话，可以接第二块屏幕，把微信放副屏

---

## 五、功能详解

### 5.1 白名单：决定回复谁

**界面入口**：主界面点 **「白名单配置」**（需要先框选好联系人列表）

打开后，程序会**自动读取你微信里的好友名单**，列出一个清单，每个人前面有个方框：

```
[√] 张三          暖心
[ ] 李四          干练同事
[√] 王五          默认：暖心
```

| 操作 | 效果 |
|---|---|
| **勾选名字前的方框** | 这个人发消息时会自动回复 |
| **取消勾选** | 不再自动回复这个人 |
| **点右边的名字（带下划线的）** | 给这个人换个"性格" |
| **点「刷新识别」** | 重新读取好友名单（新加的好友要点这个才会出现） |

**改完立刻生效**，不用重启程序。

【注意】**一个重要的坑**：只要你设置过白名单（哪怕后来全部取消勾选），程序就认为你在用"白名单模式"，此时**全部不勾选 = 谁都不回**。这是为了防止你误操作导致它对所有人乱回。

如果你真的想"回复所有人"，需要联系开发者帮你把白名单配置清空。

### 5.2 人设：让 AI 用不同性格说话

**界面入口**：主界面点 **「人设配置」**

人设就是给 AI 设定的"角色扮演剧本"。程序自带了几种，你也可以自己造。

#### 列表页

打开后看到现有的人设清单，标注了哪个是"默认"。可以：

- 点 **「编辑」** 修改某个人设
- 点 **「＋ 新增人设」** 创建一个新的

#### 编辑页

| 项目 | 怎么填 | 必填？ |
|---|---|---|
| **人设名字** | 起个名字，比如"温柔姐姐" | **必填** |
| **人设特点** | 点按钮选择，可多选（亲切/幽默/毒舌/暖心/傲娇/撒娇…）<br>选中的右上角显示红色 ×，再点一次取消 | 选填 |
| **人设身份** | 点按钮单选（老师/医生/程序员/设计师/学生…） | 选填 |
| **人设内容** | 选完特点和身份会自动生成一段文字<br>也可以自己直接改写 | 选填 |

底部三个按钮：

- **清空人设**：全部清除重填
- **保存人设**：保存（只有填了名字才能点）
- **删除人设**：删除当前人设

> 【提示】窍门：**人设内容**那一段才是真正决定 AI 怎么说话的关键。如果你觉得 AI 回复得不够像，直接手动改这段文字效果最好。比如写"你是一个说话简短、爱用emoji的女生，从不长篇大论"。

新人设建好后，去白名单界面把它分配给某个联系人就行。

### 5.3 表情包

这个功能分两部分：**发出去** 和 **收进来**。

#### 5.3.1 发出去（让 AI 自动配表情）

**原理**：AI 觉得合适的时候，会在回复里偷偷加个标记 `[表情:开心]`，程序看到这个标记就去你的表情面板点对应的表情发出去。**标记本身不会显示给对方**。

**要做的准备**（做一次就行）：

**第一步：设置情绪对应的第几个表情**

主界面点 **「表情包配置」**，看到一张表：

```
情绪名        面板第几格
开心    →     2
难过    →     5
```

意思就是：AI 说"开心"时，发你表情面板的第 2 个表情。

> 【提示】**第几格怎么数**：点开微信输入框旁的笑脸按钮，打开"我的表情"面板，**从左上角开始数，从左往右、一行一行往下数**，第一个是 1。

> 【提示】情绪多的时候中间这张表会滚动：**把鼠标移到表格上、用滚轮就能上下翻**；也可以直接把「表情包配置」窗口**拖大**，表格会自动变高。顶部的标定按钮和底部的「＋ 添加映射 / 保存映射」始终固定在外面，滚到哪儿都点得到。

**第二步：标定表情面板位置（让程序知道去哪点）**

点 **「标定表情面板（5 次点击）」**，程序会提示你依次点 5 下（点「确定」后有 1.5 秒准备时间）：

| 第几下 | 点哪里 |
|---|---|
| ① | 微信输入框左边的 **笑脸按钮**（打开表情面板） |
| ② | 面板里的 **「自定义表情」分组**（切到自定义表情页；本来就在这一页也没关系，多点一下不影响） |
| ③ | **自定义表情第一行第一格**的中心（点完面板会自动关掉，正常） |
| ④ | **再点一次 笑脸按钮**（重新打开面板，这时会停在自定义表情页） |
| ⑤ | **自定义表情第一行第二格**的中心 |

> 【提示】为什么要多点一次"自定义表情"：微信第一次打开表情面板时停在**默认表情**页，必须先切到**自定义表情**页，程序才能按你"第几格"的编号找到正确的表情。这个页签微信会记住，所以第 ④ 步重开面板时不用再切。

> 【注意】标定时会真的往当前聊天里发出 2 个表情包，**这是正常的**，标定完可以手动撤回。
> 【注意】60 秒内没点完会自动取消，可以重新点按钮再来。
> 【注意】**微信窗口移动或缩放后，坐标就失效了，必须重新标定。**

**第三步：测试**

点 **「测试发送」**，选一个情绪，看它是不是真的发了对应的表情。位置不对就调整"第几格"或重新标定。

> 【提示】**AI 不发表情怎么办**：这个完全由 AI 自己决定（它被要求"不要每条都加"）。如果发现它几乎不发，多半是情绪词起得太生僻，换成"开心/难过/生气/害羞/无语"这种常用词会好很多。

#### 5.3.2 收进来（看懂别人发的表情包）

别人发表情包时，程序看不见文字，需要有个"能看懂图片"的 AI 来描述。

**DeepSeek 看不了图**，所以要另外配一个。

在 `.env` 文件里加三行（以阿里云的通义千问为例）：

```
VISION_API_KEY=你的视觉模型密钥
VISION_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
VISION_MODEL=qwen-vl-max
```

视觉模型可以去这些地方申请（都兼容）：

- 阿里云百炼（通义千问 VL）
- 智谱 AI（GLM-4V）
- OpenAI（GPT-4o）

**不配也能用**：收到表情包时会当成 `[表情包]` 占位符，AI 照样能回（只是不知道图上画了啥）。

> 【提示】同一个表情包只分析一次，结果会缓存起来，不重复花钱。

### 5.4 长期记忆：让 AI "记住"人

程序会自动分析聊天内容，把每个人的重要信息记下来（存在根目录的 `memory.json`）：

- 家乡、工作地、年龄、生日
- 喜好、习惯
- 最近在做的事

**回复时会自动参考这些信息**，比如对方上次说"下周三考试"，过几天 AI 可能会主动问"考得怎么样"。

**规则**：

- 每次回复后有机会更新，但**最快 6 小时一次**（避免频繁花钱）
- 只分析最近 24 小时的聊天
- 每条记忆有长度上限，不会无限膨胀

不需要你做任何操作，全自动。

### 5.5 测试模式：先试试不真发

主界面有个 **「测试模式（不真的发送）」** 复选框。

勾上之后：

- 程序照常读消息、照常让 AI 想回复
- **但不会真的发出去**，只在日志里显示"打算说什么"
- AI 也不消耗额度（不调用付费接口）

**强烈建议第一次用的时候先勾上**，看着日志跑通了、确认没问题，再取消勾选正式用。

### 5.6 「请我喝杯咖啡」

界面右上角有个 **「☕ 请我喝杯咖啡」**，这是作者放收款码的地方。

- 点标题或右边 **✕** 可以收起/展开二维码
- 点二维码会放大显示，里面有「打开原图」按钮（扫不上就用原图扫）

打赏全凭自愿，不影响任何功能。

不想看到它：在 `.env` 里加一行 `SHOW_DONATE=false`。

---

## 六、出了问题怎么办

### 6.1 启动就失败

| 看到的现象 | 原因 | 怎么办 |
|---|---|---|
| `ModuleNotFoundError: No module named 'xxx'` | 依赖没装全 | 重新执行 `pip3 install -r requirements.txt` |
| `python3: command not found` | Python 没装好或没配环境 | 重装 Python（Windows 注意勾 Add to PATH） |
| `Permission denied` | 文件权限问题 | Mac 上试试 `chmod +x wechatbot_gui.py` |
| 提示"缺少屏幕录制权限" | Mac 没授权 | 见 2.6 节，**记得完全退出终端再重开** |
| 窗口一闪就没了 | 命令有误 | 用 `python3` 而不是双击文件；检查是否在正确的文件夹里 |

### 6.2 认不出消息 / 不回复

| 现象 | 排查方向 |
|---|---|
| 一直显示"聊天区无变化" | 框选区域不对，重新框一次 |
| 显示"最后一条为我方消息" | 区域框得太宽，把输入框或头像框进去了 → 重新框，**确保不含头像和输入框** |
| 显示"无法识别当前会话联系人" | 联系人列表框选不准，或高亮色没识别出来 → 重新框选列表区域；试试把微信切成浅色模式 |
| 认错人、串到别的好友 | 同上；另外检查白名单里名字是否重复 |
| 别人发表情包没反应 | 表情包检测需要聊天区框选正确（不含头像/输入框）；没配视觉模型也会降级处理 |

### 6.3 回复发不出去

| 现象 | 原因与解决 |
|---|---|
| 日志显示"AI回答"但没有"已发送" | 光标不在输入框 → **手动点一下微信输入框**再启动 |
| 反复显示"发送未确认成功" | 同上；或者有弹窗挡住了微信窗口 |
| 消息发到了别的地方 | 微信窗口没在最前面 → 程序会自动切，但如果你手动切走了就会出错 |
| 回复内容里带着"（微笑）"这类描述 | 旧版本问题，现在会自动删掉；如还有请更新程序 |

### 6.4 认字慢 / 卡顿

- OCR（认字引擎）本身比较耗资源，老电脑会慢一些
- 框选区域越大，处理越慢 → **尽量只框必要的范围**
- 关掉其他占资源的软件
- 第一次慢是正常的，之后会快一点

### 6.5 AI 回复质量不好

| 问题 | 调整方法 |
|---|---|
| 回复太长 | 人设内容里加"说话简短，每次不超过两句话" |
| 太客套生疏 | 人设里写"像关系很好的朋友，口语化，不用敬语" |
| 语气不对 | 直接改人设里那段文字，最有效 |
| 不会接话 | 检查是否开启了记忆功能（`MEMORY_INJECT_REPLY=true`） |
| 不看场合乱回 | 检查白名单，只给需要的人开启 |

### 6.6 完全没反应，日志一片空白

按顺序检查：

1. 状态栏显示"运行中"了吗？没有的话点启动
2. 对方是不是白名单里的人？
3. 微信窗口是不是被别的窗口挡住了？
4. 框选区域还有效吗？（微信窗口挪过位置就要重框）
5. 手动点一下输入框了吗？

---

## 七、重要提醒

### 安全方面

- 程序**不修改微信文件**、不使用外挂插件，纯粹模拟看屏和打字，**理论上没有封号风险**
- 【注意】但**回复频率过高可能引起腾讯风控**（跟你自己手速太快一样）。建议不要同时给几十个人自动回复
- 🔒 `.env` 文件里有你的密钥，**绝对不要发给别人、不要发到网上、不要提交到公开仓库**
- 🔒 聊天记录保存在本地（`chat_history` 文件夹），别人用你的电脑就能看到，注意隐私

### 使用方面

- 【注意】**运行期间不要动鼠标键盘**，会打断它的操作
- 【注意】**不要遮挡、最小化微信窗口**
- 【注意】**不要锁屏 / 让电脑休眠**
- 【提示】换显示器、改分辨率、挪动微信窗口后，**必须重新框选区域**（并且重新标定表情面板）
- 【提示】建议先用「测试模式」跑通，再正式使用
- 【提示】重要对话建议还是自己回，AI 只是辅助

### 费用方面

- 每次调用 AI 都要花钱（DeepSeek 很便宜，但累积起来也是钱）
- 长期记忆功能每 6 小时会调用一次 AI 分析
- 视觉模型（看表情包）是另一笔费用
- 不用的时候记得关掉程序

---

## 八、附录：配置速查表

`.env` 文件里可以改的设置（一般**只需要填 API Key**，其他保持默认即可）：

| 配置项 | 默认值 | 作用 |
|---|---|---|
| `OPENAI_API_KEY` | 空 | **必填**，你的 DeepSeek 密钥 |
| `OPENAI_BASE_URL` | `https://api.deepseek.com` | 接口地址，一般不用改 |
| `OPENAI_MODEL` | `deepseek-chat` | 模型名，一般不用改 |
| `DRY_RUN` | `false` | 改成 `true` 就不调 AI、不发消息，纯调试 |
| `AUTO_ACTIVATE_WECHAT` | `true` | 发送前自动把微信切到最前 |
| `SHOW_DONATE` | `true` | 改成 `false` 隐藏右上角收款码 |
| `MEMORY_INJECT_REPLY` | `true` | 回复时是否带上对这个人的记忆 |
| `ENABLE_STICKERS` | `true` | 改成 `false` 完全关闭表情包功能 |
| `VISION_API_KEY` | 空 | 视觉模型密钥（看别人表情包用，可选） |
| `VISION_BASE_URL` | 空 | 视觉模型地址（可选） |
| `VISION_MODEL` | 空 | 视觉模型名称（可选） |
| `MEMORY_ANALYZE_INTERVAL_HOURS` | `6` | 记忆更新的最短间隔（小时） |

### 文件说明

| 文件或文件夹 | 是什么 |
|---|---|
| `wechatbot_gui.py` | **图形界面入口**（推荐的启动方式） |
| `wechatbot.py` | 命令行版本入口（不推荐新手用） |
| `.env` | 你的密钥和设置 |
| `config/personas.json` | 人设和白名单（**建议用界面改，不要手改**） |
| `config/stickers.json` | 表情包配置（**建议用界面改**） |
| `config/regions.json` | 框选区域记忆（界面自动维护） |
| `chat_history/` | 聊天记录，按联系人分文件 |
| `memory.json` | 对每个联系人的长期记忆 |

### 命令行版本（进阶用户）

如果不想要图形界面，也可以用命令行版：

```
python3 wechatbot.py
```

它会让你**依次点 4 个点**来框选区域（列表左上角→右下角，聊天区左上角→右下角），之后就在终端里滚日志。`Ctrl + C` 停止。

> 【注意】命令行版**不会记住框选区域**，每次启动都要重新点 4 下。所以推荐用图形界面版。

---

**祝使用愉快！有问题欢迎反馈。**
